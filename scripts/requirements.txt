# Dependencies for scripts/ — install with: pip install -r scripts/requirements.txt
openpyxl>=3.1
xlsxwriter>=3.1
pandas>=2.0
python-calamine>=0.3  # only needed for /stock-opname-teknisi's WebSMS device-list files
